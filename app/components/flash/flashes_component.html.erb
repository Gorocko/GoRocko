<%# if current_user != true_user %>
<!--  <div class="flex items-center justify-center px-2 py-1 text-sm text-blue-200 bg-blue-600 font-semibold" role="alert">-->
<!--    <span class="mr-2">Logged in as <b><%#= link_to "#{current_user.name} (#{current_user.email})", admin_user_path(current_user), class: "text-blue-200 underline hover:text-blue-100" %></b></span>-->
    <%#= button_to admin_user_impersonate_path(current_user), method: :delete, form_class: "inline-block", class: "ml-3 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 btn-small" do %>
<!--      Logout-->
    <%# end %>
<!--  </div>-->
<%# end %>
<%= content_tag(:div, class: "flash_messages",
                id: 'flash_messages') do %>
  <% @flash.each do |type, message| %>
      <%= render Flash::FlashMessageComponent.new(type: type, msg: message) %>
  <% end %>
<% end %>
